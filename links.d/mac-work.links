# mac-work — Copilot-oriented; no claude settings.
# Copilot CLI/VS Code global instruction + skills discovery paths are wired here
# once verified on the work machine (see agents/AGENTS.md).
# Completion notifications come from the common layer (~/.copilot/hooks/notify.json).
